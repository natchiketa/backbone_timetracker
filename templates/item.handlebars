<div class="view">
    <div class="itemstate"><i class="{{{ternary completed "icon-ok" "icon-time"}}}"></i></div>
    <div class="timevals">
        <div class="start timestamp">{{{dateformat start}}}</div>
        <div class="stop timestamp {{{ternary completed "" "running"}}}">{{{dateformat stop}}}</div>
    </div>
    <button class="destroy"></button>
</div>
